from json_util import extract_json
import json

def process_answer(answers):
    # json_data = extract_json(answer)
    # if json_data:
    #     print(json.dumps(json_data))
    #     cleaned_answer = answer.replace(json.dumps(json_data), "")
    #     return cleaned_answer
    result = answers
    return result

